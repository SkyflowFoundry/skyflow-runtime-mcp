/**
 * Test stub for src/generated/ui-html.ts (a gitignored build artifact
 * produced by `pnpm build:ui-imports`). Substituted via a resolve alias in
 * vitest.config.ts so integration tests can import src/server.ts without
 * building the UI first.
 */
export const deIdentifyHtml = "<!doctype html><title>de-identify stub</title>";
export const reIdentifyHtml = "<!doctype html><title>re-identify stub</title>";
export const deIdentifyFileHtml = "<!doctype html><title>de-identify-file stub</title>";
